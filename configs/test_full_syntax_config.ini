; Test configuration for full architecture syntax
[model]
; Test: Full syntax with explicit layer declarations
architecture = Sequential(Linear(2,8), ReLU(), Linear(8,1), Sigmoid())

[training]
epochs = 50
batch_size = 4
learning_rate = 0.1
optimizer = sgd

[data]
train_data = csv/xor_train.csv
train_labels = csv/xor_labels.csv
val_data = csv/xor_train.csv
val_labels = csv/xor_labels.csv

[loss]
loss_function = bce

[logging]
print_every = 50
save_model = 
